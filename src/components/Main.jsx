export default function Main() {
    return (
        <main>
            <form className="add-ingredient-form">
                <input className="" type="text" aria-label="Add Ingredient" placeholder="e.g. oregano"/>
                <button>Add Ingredient</button>
            </form>
        </main>
    )
}